package com.feseek.api;

public class EventSearchRestController 
{
	/*
	@Autowired
	private EventsRepository repository;
	//イベント検索(キーワード)
	@GetMapping("/api/event/search")
	protected List<Event> search(@RequestBody Event event)
	{
		String keyword = event.getKeyword();
		
		return repository.//ここに検索の関数名
				 ("%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				 ,"%" + keyword + "%"
				);
	}
	*/
}
